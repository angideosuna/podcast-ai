# ============================================
# PodCast.ai — Variables de entorno
# ============================================
# Copia este archivo como .env.local y rellena los valores.
# NUNCA subas .env.local al repositorio.

# --- Noticias (obligatoria) ---
GNEWS_API_KEY=                    # https://gnews.io — plan gratis: 100 req/dia

# --- News Agent (paso 4) ---
NEWSAPI_KEY=                      # https://newsapi.org — plan gratis: 100 req/dia

# --- IA / Generacion de guiones (obligatoria) ---
ANTHROPIC_API_KEY=                # https://console.anthropic.com

# --- Audio / Text-to-Speech (opcional) ---
ELEVENLABS_API_KEY=               # https://elevenlabs.io — sin esto se usa Web Speech API del navegador
ELEVENLABS_VOICE_ID=              # Opcional: ID de voz personalizada. Si no se pone, usa voces por defecto

# --- Supabase (obligatorias) ---
NEXT_PUBLIC_SUPABASE_URL=         # URL de tu proyecto: https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=    # Clave publica (anon key)
SUPABASE_SERVICE_ROLE_KEY=        # Clave de servicio (solo para operaciones de servidor)

# --- Vercel KV (Rate Limiting persistente) ---
KV_REST_API_URL=                  # URL de tu Vercel KV store
KV_REST_API_TOKEN=                # Token de tu Vercel KV store

# --- Vercel Cron Jobs ---
CRON_SECRET=                      # Secret para autenticar las rutas cron. Genera uno con: openssl rand -base64 32
